

git init => crea la carpeta de git en el proyecto que estamos usando.
            (es una carpeta oculta - trabaja internamente)


git status => para ver los archivos que tenemos

git add . => para decir que archivo agregar

git log => te dice cuales fueron los commit

git checkout -- index.html => para volver atrás en el index.html

git diff => te muestra las diferencias de lo que uno arreglo

git branch => creamos una rama
git branch -d => borramos una rama
git checkout nombreRama => Vamos a la rama

git log => muestra los commit
git reset => vamos al commit que queremos 







fazt web: 
https://www.youtube.com/watch?v=HiXLkL42tMU